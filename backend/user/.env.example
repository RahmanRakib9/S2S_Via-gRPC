# Example Environment Variables
# ==============================
# This is a template file. DO NOT commit your actual credentials to version control!
#
# SETUP INSTRUCTIONS:
# -------------------
# 1. Create .env.development file for development environment:
#    Copy this file and rename it to .env.development
#    Fill in your actual values for development
#
# 2. Create .env.prod file for production environment:
#    Copy this file and rename it to .env.prod
#    Fill in your actual values for production
#
# IMPORTANT:
# - .env.development and .env.prod files are already added to .gitignore
# - Never commit actual credentials or secrets to version control
# - The application automatically loads the correct .env file based on NODE_ENV:
#   - If NODE_ENV=development, it loads .env.development
#   - If NODE_ENV=production, it loads .env.prod
#
# USAGE:
# ------
# Development: NODE_ENV=development npm run dev
# Production:  NODE_ENV=production npm start

NODE_ENV=development
PORT=3001

# MongoDB Connection
# Option 1: MongoDB Atlas (mongodb+srv://)
# MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/database_name?retryWrites=true&w=majority&appName=Cluster0

# Option 2: Standard Remote MongoDB (mongodb://)
# MONGODB_URI=mongodb://username:password@host:27017/database_name?authSource=admin&ssl=true

MONGODB_URI=

# JWT Secret
# IMPORTANT: Use a strong, randomly generated secret in production!
JWT_SECRET=your-super-secret-jwt-key-change-in-production
